name: build

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: "${{ matrix.SECCOMP == '1' && 'seccomp' || 'no-seccomp' }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        SECCOMP: [ 0, 1 ]

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v3
        with:
          fetch-depth: 0

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -qq clang-tools curl gdb lcov libarchive-dev libtalloc-dev pkg-config sloccount strace swig uthash-dev python3-dev lzop

      - name: Gather analytics
        run: sloccount --details .

      - name: Build elf loader, proot, and care
        run: |
          make -C src clean loader.elf loader-m32.elf build.h proot care

      - name: Execute test suite
        continue-on-error: true
        timeout-minutes: 10
        run: |
          env ${{ matrix.SECCOMP == '0' && 'PROOT_NO_SECCOMP=1' || '' }} PATH=/bin:/usr/bin:/sbin:/usr/sbin:$PWD/src make -C test -j $(nproc) QUIET_LOG=$PWD/test.log

      - name: Output test log
        if: always()
        continue-on-error: true
        run: ([ -f test.log ] && cat test.log) || true
